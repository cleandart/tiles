part of library;

/**
 * Define type for component factory ( function which return new instance of Component with props )
 */

typedef ComponentInterface ComponentFactory([NodeInterface node, PropsInterface props]);
