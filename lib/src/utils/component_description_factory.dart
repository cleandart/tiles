part of tiles;

/**
 * Define type for component factory (function which return new instance of Component with props)
 */

typedef ComponentDescription ComponentDescriptionFactory([dynamic props, dynamic children, dynamic key]);
